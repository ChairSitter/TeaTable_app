<main>
    <div>
        <h2>
            <%= @post.title %>
        </h2>
        <p>
            <%= image_tag(@post.user.profile_picture) %>
        </p>
        <%= @post.body %>
        Post by <%= @post.user.username %>
    </div>
</main>